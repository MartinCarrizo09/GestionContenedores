# ============================================================
# VARIABLES DE ENTORNO - TPI Backend
# ============================================================
#
# INSTRUCCIONES:
# 1. Copia este archivo como .env en la raíz del proyecto
# 2. Configura tus credenciales
# 3. NO subas el archivo .env a Git (ya está en .gitignore)
#
# ============================================================

# ========== POSTGRESQL LOCAL (DOCKER) ==========
# Contraseña de la base de datos PostgreSQL
POSTGRES_PASSWORD=admin123

# ========== KEYCLOAK (AUTENTICACIÓN) ==========
# Contraseña del administrador de Keycloak
KEYCLOAK_ADMIN_PASSWORD=admin123

# ========== GOOGLE MAPS API ==========
# API Key para calcular rutas y distancias
GOOGLE_MAPS_API_KEY=AIzaSyYourActualKeyHere

# ========== CONFIGURACIÓN DE LOGS ==========
LOG_LEVEL=INFO

# ========== NOTAS IMPORTANTES ==========
# 
# 1. PostgreSQL corre en Docker en localhost:5432
# 2. Keycloak corre en localhost:9090
# 3. API Gateway es la entrada única en localhost:8080
# 4. Los microservicios corren en puertos internos:
#    - servicio-gestion  → 8081 (acceso vía Gateway)
#    - servicio-flota    → 8082 (acceso vía Gateway)
#    - servicio-logistica → 8083 (acceso vía Gateway)
# 5. Cada microservicio usa su propio schema:
#    - servicio-gestion  → schema 'gestion'
#    - servicio-flota    → schema 'flota'
#    - servicio-logistica → schema 'logistica'
#
# ============================================================
